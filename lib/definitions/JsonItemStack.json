{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "JsonItemStack",
  "$id": "JsonItemStack.json",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "description": "The asset location code of the block or item"
    },
    "type": {
      "description": "Block or Item?",
      "allOf": [
        {
          "$ref": "./EnumItemClass.json#"
        }
      ]
    },
    "attributes": {
      "description": "Tree Attributes that should be attached to the resulting itemstack.",
      "allOf": [
        {
          "$ref": "../assets/blocktypes-attributes.json#/properties/attributes"
        }
      ]
    },
    "quantity": {
      "type": "integer",
      "format": "int32",
      "description": "Alias of StackSize. No real need to use this instead of it."
    },
    "stackSize": {
      "type": "integer",
      "format": "int32",
      "description": "Amount of items in this stacks"
    }
  }
}
